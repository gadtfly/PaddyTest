.deck{id: dom_id(deck)}
  %h2= link_to deck.title, deck
  %ul
  - deck.cards.each do |card|
    %li{id: dom_id(card)}= link_to card.title, card
  =link_to 'Delete', deck, method: :delete, remote: true